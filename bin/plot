#!/usr/local/bin/zsh -u

FILE=$(readlink -f $1)
WITH=$2
cd `dirname $0`

SCRIPT=$(cat <(cat _plot/plot.pl) <(_plot/gen $FILE $WITH))
echo $SCRIPT
#gnuplot -p <(cat _plot/plot.pl <(_plot/gen $FILE $WITH))
echo "$SCRIPT" | gnuplot -p
#./_plot/gen $FILE $WITH | gnuplot -p
